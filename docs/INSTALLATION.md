# Linux installation

Download the release tarball, extract it, and run `./install.sh`. It installs under `~/.local/share/archive-scout`, creates `~/.local/bin/archive-scout`, and adds a desktop entry without administrator access. `Run Archive Scout.sh` also starts the unpacked portable copy. The release targets 64-bit glibc-based Linux systems comparable to Ubuntu 22.04 or newer.
